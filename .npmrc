# Configuración de npm para mejorar la instalación de Prisma
# Aumentar timeout para descargas grandes (5 minutos)
fetch-timeout=300000
fetch-retries=5
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000

# Nota: El script install.ps1 configura automáticamente las variables de entorno
# necesarias para evitar la descarga de engines durante npm install
